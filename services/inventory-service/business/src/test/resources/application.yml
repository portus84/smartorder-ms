spring:
  datasource:
    embedded-database-connection: h2
  jpa:
    generate-ddl: true
    show-sql: true
    properties:
      hibernate.globally_quoted_identifiers: true

logging:
  level:
    root: debug
